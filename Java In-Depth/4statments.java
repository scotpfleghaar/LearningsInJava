public class 4statements {
    public static void main(String[] args) {
       

    }
    // field declarations
    static int count = 25;

    // Method declarations
    // Constructor delcations
    // Nested class and interface declarations
    // Instance and static initializers


    //The following produce a Compilation error becuase they are in the body of the class
    // - control flow statements (if, for, while)
    // Expression statments (count = 26);
}
